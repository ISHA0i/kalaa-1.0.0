{
  "devDependencies": {
    "cross-env": "^7.0.3"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "@fortawesome/fontawesome-free": "^6.x.x"
  },
  "scripts": {
    "start": "cross-env PORT=3001 react-scripts start && echo Frontend running on port %PORT%",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "both": "concurrently \"npm run start\" \"npm run backend\""
  },
  "homepage": "."
}

